@model IEnumerable<icbf_app.Models.MadreComunitaria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaNacimientoMadre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdJardinNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdUsuarioNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaNacimientoMadre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdJardinNavigation.IdJardin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdUsuarioNavigation.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMadreComunitaria">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdMadreComunitaria">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMadreComunitaria">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
